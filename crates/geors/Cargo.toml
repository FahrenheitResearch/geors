[package]
name = "geors"
version = "0.1.0"
edition.workspace = true
description = "Pure Rust geodesic calculations — Karney's algorithm, great-circle, polygon area"

[dependencies]
geo-math = { path = "../geo-math" }
geographiclib-rs = "0.2"
